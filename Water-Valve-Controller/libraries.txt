# Water Valve Controller - Required Libraries

# Core ESP32 Arduino framework (includes WiFi and ESP-NOW)
ESP32 Arduino Core
Version: 3.3.1 (tested)
Source: https://github.com/espressif/arduino-esp32

# Standard Arduino libraries
WiFi.h       (bundled with ESP32 core)
esp_now.h    (bundled with ESP32 core)

# Optional (depending on your setup)
Arduino.h    (comes by default with Arduino core)
